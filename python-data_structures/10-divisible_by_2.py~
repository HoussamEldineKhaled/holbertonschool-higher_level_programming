#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    mult_list = []
    for i in range(0, len(my_list)):
        if my_list[i] % 2 == 0:
            mult_list[i] = True
        else:
            mult_list[i] = False
    return mult_list
